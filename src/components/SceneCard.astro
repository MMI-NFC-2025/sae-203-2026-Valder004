---
// Composant SceneCard - Carte d'une scène
const { nom, description, couleur = '#e63946' } = Astro.props;
---

<div class="bg-[#0a0a0a] p-8 text-center">
    <h3 class="text-2xl font-bold mb-4" style={`color: ${couleur}`}>{nom}</h3>
    <p class="text-gray-400">{description}</p>
</div>
